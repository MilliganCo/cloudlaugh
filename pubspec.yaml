name: cloudlaugh
description: Random canned laughter player
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
